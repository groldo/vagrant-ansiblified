all:
  vars:
    scenario: virtualbox
  children:
    hyperv:
      hosts:
        hostone:
    virtualbox:
      hosts:
        hosttwo:
    kubernetes:
      children:
        masternodes:
          hosts:
            masternode:
        workernodes:
          hosts:
            workerone:
